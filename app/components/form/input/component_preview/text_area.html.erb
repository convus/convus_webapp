<%= form_with(url: "#", builder: SethTemplateFormBuilder) do |f| %>
  <%= render(Form::Input::Component.new(form_builder: f, attribute: :name, kind: :text_area, html_options: {placeholder: "Write something...", rows: 4})) %>
<% end %>
