default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("DB_POOL") { 1000 } %>

default_local: &default_local
  <<: *default
  port: <%= ENV.fetch("PGPORT", 5432) %>
  <% if ENV["CI"] %>
  host: 127.0.0.1
  username: postgres
  password: password
  <% else %>
  host: <%= ENV.fetch("PGHOST", "localhost") %>
  username: <%= ENV["PGUSER"] %>
  password: <%= ENV["PGPASSWORD"] %>
  <% end %>

development:
  <<: *default_local
  database: convus_reviews_development<%= "_#{ENV["WORKSPACE_ID"]}" if ENV["WORKSPACE_ID"] %>

test:
  <<: *default_local
  database: convus_reviews_test<%= "_#{ENV["WORKSPACE_ID"]}" if ENV["WORKSPACE_ID"] %><%= ENV["TEST_ENV_NUMBER"] %>

production:
  <<: *default
  host: <%= ENV.fetch("POSTGRESQL_ADDRESS", "localhost") %>
  username: <%= ENV.fetch("POSTGRESQL_USERNAME", "convus_webapp") %>
  password: <%= ENV["POSTGRES_PASSWORD"] %>
  database: convus_webapp_production
